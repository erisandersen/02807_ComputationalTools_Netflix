# 02807_ComputationalTools_Netflix

git status <br />
git add .  <br />
git commit -m "<message_here>" <br />
git pull origin main <br />
git push origin main <br />

!! If it doesn't want to commit: Save the file (strg s or cmd s) and then try again. That should fix it!!

